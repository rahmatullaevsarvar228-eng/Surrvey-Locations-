<?xml version="1.0" encoding="utf-8"?>
<!-- Разрешает .NET загружать библиотеки окна, даже если Windows пометила их
     как скачанные из интернета (второй рубеж после unblock_bundle). -->
<configuration>
  <runtime>
    <loadFromRemoteSources enabled="true"/>
  </runtime>
</configuration>
